name: work191-e2e-fail
on:
  push:
jobs:
  fail:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: intentionally-fail
        run: echo "e2e intentional failure"; exit 1
